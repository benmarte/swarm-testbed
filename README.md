# swarm-testbed

E2E testbed for [benmarte/swarm](https://github.com/benmarte/swarm). This repo contains a minimal bash calculator (`calc.sh`) and BATS test suite used to exercise the full swarm pipeline end-to-end — from issue triage through automated fix, review, and docs — without the noise of a real project.
