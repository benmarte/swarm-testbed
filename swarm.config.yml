notify:
  # Buzz (Nostr/NIP-29) — relay URL + bot key are secrets; the channel UUID is behavior.
  buzz_channel: "275b679c-545b-4445-b228-536cf19159cb"
  # Slack — bot-token mode: SWARM_SLACK_BOT_TOKEN secret + channel ID below.
  slack_channel: "C0B3P2Q39LN"
  # Discord — bot-token mode: SWARM_DISCORD_BOT_TOKEN secret + channel ID below.
  discord_channel: "1512810543172554877"

develop:
  adapter: headless

# Base URL for the local openai-compat endpoint used by decision roles.
# run 3: deepseek-v4-flash served at http://localhost:9000/v1
SWARM_LLM_BASE_URL: "http://localhost:9000/v1"

qa:
  required_checks:
    - ci

runner:
  label: swarm-agent

sweeper:
  schedule: "0 2 * * *"
